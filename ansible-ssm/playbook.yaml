---
- name: Configure web servers via AWS SSM
  hosts: web
  become: true
  gather_facts: false

  tasks:
    - name: Gather minimal facts
      setup:
        gather_subset:
          - min

  roles:
    - nginx
