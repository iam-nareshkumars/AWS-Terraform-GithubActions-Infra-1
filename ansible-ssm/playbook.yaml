---
- name: Configure web servers via AWS SSM
  hosts: web
  become: true
  gather_facts: false

  pre_tasks:
    - name: Gather minimal facts
      setup:
        gather_subset:
          - min

  roles:
    - nginx
